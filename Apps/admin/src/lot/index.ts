export interface Lot {
    id: string,
    name: string,
    zone: string,
    address: string,
    latitude: number,
    longitude: number,
    capacity: number,
    availableSpots: number,
    isActive: boolean,
    type: string,
    created: string,
    updated: string
}
